# dependencies
node_modules/

# NOTE: lib/ (built bundles) is intentionally COMMITTED so git-source installs
# (`dsh plugin add github:mill413/dsh-mcp-manager`) work without a build step.
# Rebuild with `pnpm build` after source changes and commit the new lib/.

# logs & editor noise
*.log
.DS_Store
Thumbs.db
.vscode/
.idea/

# temp commit messages
.commitmsg*

