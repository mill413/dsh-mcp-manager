# Install policy lives in pnpm-workspace.yaml. The @deepseek-ai/* packages are
# pinned in devDependencies for type-check/build and provided by the DeepSeek
# Harness host at load time (declared in peerDependencies).
